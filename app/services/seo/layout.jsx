export const metadata = {
  title: "SEO – Maik Bock",
  description: "Content and structure optimization so search engines — and people — actually click.",
};

export default function SeoLayout({ children }) {
  return children;
}
