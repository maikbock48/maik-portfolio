export const metadata = {
  title: "Logo Design – Maik Bock",
  description: "Bold, memorable identities — crafted with a custom logo-generator tool.",
};

export default function LogoDesignLayout({ children }) {
  return children;
}
